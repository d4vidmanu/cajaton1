package repository;

import domain.SalonEtiqueta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SalonEtiquetaRepository extends JpaRepository<SalonEtiqueta, Long> {
}